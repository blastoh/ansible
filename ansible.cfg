[defaults]
inventory = inventory
private_key_file = ~/.ssh/ansible
remote_user = blasto  
# Silence the python warning
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = True
become_timeout = 30
[ssh_connection]
pipelining = True
